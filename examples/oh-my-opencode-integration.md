# oh-my-opencode + agent-teams 통합 예시

## 설정

```json
{
  "plugin": [
    "oh-my-opencode",
    "./plugins/agent-teams"
  ]
}
```

## 사용 시나리오

---

### 시나리오 1: Sisyphus가 보안 리뷰 팀 스폰

**사용자 프롬프트:**
```
ulw auth.ts 파일 보안 검토해줘
```

**Sisyphus 동작 플로우:**

```
1. Sisyphus가 파일 읽기
   └─ read("auth.ts")

2. 보안 검토를 위해 team-spawn 호출
   └─ team-spawn preset="security" teamName="auth-review" task="auth.ts 보안 검토"

3. 플러그인 응답:
   ## Team "auth-review" Created 🔀

   **Team ID**: team-1708765432100
   **Preset**: security

   ### Agents (2)
   - **security-auditor** (Security Specialist) ✓
   - **devil-s-advocate** (Critical Thinker) ✓

4. team-discuss로 병렬 분석
   └─ team-discuss teamId="team-1708765432100" topic="SQL injection, XSS, 인증 우회"

5. 결과 종합 및 사용자에게 보고
```

---

### 시나리오 2: 버그 디버깅 with 팀

**사용자 프롬프트:**
```
ulw 결제 API에서 가끔 타임아웃 발생하는데 원인 찾아줘
```

**플로우:**

```
1. Sisyphus가 코드베이스 탐색
   └─ call_omo_agent("explore", "payment API timeout")

2. 디버그 팀 스폰
   └─ team-spawn preset="debug" teamName="payment-debug" task="결제 API 타임아웃 원인 분석"

3. 토론 시작
   └─ team-discuss teamId="team-xxx" topic="타임아웃 원인: DB 커넥션? 외부 API? 레이스 컨디션?"

4. debugger 가설:
   - DB 커넥션 풀 고갈 가능성
   - 외부 결제 게이트웨이 응답 지연
   - 타임아웃 설정 값 문제

5. devil-s-advocate 반론:
   - 로드 밸런서 설정 확인 필요
   - 재시도 로직 누락 가능성
   - 캐시 미스로 인한 폭주

6. Sisyphus가 최종 수정안 구현
```

---

### 시나리오 3: 자연어 팀 요청

**사용자 프롬프트:**
```
이 기능 구현할 때 프론트엔드랑 백엔드 팀 짜서 병렬로 작업해줘
```

**플로우:**

```
1. Sisyphus가 team-auto 사용 (자연어 감지)
   └─ team-auto request="프론트엔드랑 백엔드 팀 짜서 병렬로 작업"

2. 플러그인이 "구현" 감지 → implementation 프리셋 선택
   └─ backend-developer, frontend-developer, test-automator, devil-s-advocate

3. 팀 생성 결과:

   ## 🔀 팀 생성

   **프리셋**: implementation
   **Team ID**: team-xxx

   ### 팀원
   - **backend-developer**
   - **frontend-developer**
   - **test-automator**
   - **devil-s-advocate**

4. Sisyphus가 각 에이전트에게 작업 분배
   - backend-developer → API 설계
   - frontend-developer → UI 컴포넌트
   - test-automator → 테스트 케이스
   - devil-s-advocate → 리뷰 및 반론

5. 결과 취합 후 하나의 PR로 통합
```

---

### 시나리오 4: 코드 리뷰 with 다중 관점

**사용자 프롬프트:**
```
/user-service 리팩토링한 코드 팀 리뷰해줘
```

**플로우:**

```
1. Sisyphus가 변경사항 파악
   └─ git diff, 파일 읽기

2. 리뷰 팀 스폰
   └─ team-spawn preset="review" teamName="user-service-review" task="리팩토링 코드 리뷰"

3. 병렬 리뷰:
   - code-reviewer: 코드 품질, 가독성, 패턴 준수
   - security-auditor: 보안 취약점, 데이터 노출
   - devil-s-advocate: 엣지 케이스, 예외 상황

4. 토론 결과:

   **code-reviewer**:
   - MEDIUM: 중복 코드 발견 (line 45-52)
   - Score: 7/10

   **security-auditor**:
   - HIGH: 사용자 입력 검증 누락
   - CRITICAL: SQL 쿼리에 직접 문자열 삽입

   **devil-s-advocate**:
   ### What's Missing
   1. 에러 처리 일관성
   2. 로깅 전략
   3. 성능 고려사항

5. Sisyphus가 수정사항 적용
```

---

### 시나리오 5: 아키텍처 설계 with 토론

**사용자 프롬프트:**
```
마이크로서비스 아키텍처로 전환하는 계획 세워줘
```

**플로우:**

```
1. Sisyphus가 현재 아키텍처 분석
   └─ call_omo_agent("librarian", "microservices patterns")

2. 계획 팀 스폰
   └─ team-spawn preset="planning" teamName="microservices-migration" task="마이크로서비스 전환 계획"

3. planner 분석:
   - 현재 모놀리식 구조 분석
   - 도메인 경계 식별
   - 전환 단계 제안

4. devil-s-advocate 반론:
   - 분산 시스템 복잡성
   - 네트워크 레이턴시
   - 데이터 일관성 문제
   - 운영 오버헤드

5. 토론 결과를 바탕으로 최종 계획 수립
```

---

## 도구 매핑

| oh-my-opencode | agent-teams | 용도 |
|----------------|-------------|------|
| `call_omo_agent` | `team-spawn` | 에이전트/팀 생성 |
| 백그라운드 실행 | `team-discuss` | 병렬 작업 |
| - | `team-status` | 상태 확인 |
| - | `team-shutdown` | 종료 |
| `ulw` 키워드 | `team-auto` | 자연어 요청 |

## 프롬프트 템플릿

### 보안 리뷰
```
ulw [파일/기능] 보안 검토 팀 짜서 해줘
```

### 버그 디버깅
```
ulw [문제] 원인 찾아줘 - 디버그 팀으로
```

### 기능 구현
```
ulw [기능] 구현해줘 - 프론트엔드 백엔드 팀 짜서
```

### 코드 리뷰
```
ulw [코드] 리뷰 팀이랑 검토해줘
```

### 아키텍처 설계
```
ulw [설계] 계획 팀이랑 세워줘
```

---

## 실제 동작 확인

```bash
# OpenCode 실행
opencode

# 프롬프트 입력
ulw auth.ts 보안 검토 팀 짜서 해줘

# 예상 출력:
# 1. Sisyphus가 파일 분석
# 2. team-spawn으로 security 팀 생성
# 3. team-discuss로 병렬 분석
# 4. 결과 종합 보고
```
