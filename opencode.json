{
  "$schema": "https://opencode.ai/config.json",
  "plugin": [
    "opencode-sessions",
    "@opencode-ai/plugin-agent-teams"
  ],
  "model": "zai-coding-plan/glm-4.7",
  "small_model": "zai-coding-plan/glm-4.5-flash",
  "permission": {
    "edit": "allow",
    "bash": {
      "*": "allow"
    },
    "webfetch": "allow",
    "websearch": "allow"
  },
  "command": {
    "review": {
      "template": "Review code changes in $ARGUMENTS for best practices, security, and performance issues.",
      "description": "Code review",
      "agent": "code-reviewer"
    },
    "commit": {
      "template": "Create a well-structured git commit for current staged changes. Write a clear commit message following conventional commits.",
      "description": "Smart commit"
    },
    "debug": {
      "template": "Debug following issue: $ARGUMENTS. Use systematic debugging methodology.",
      "description": "Debug an issue",
      "agent": "debugger"
    },
    "test": {
      "template": "Run test suite and analyze failures. Suggest fixes for failing tests. $ARGUMENTS",
      "description": "Run and analyze tests"
    },
    "plan": {
      "template": "Create an implementation plan for: $ARGUMENTS. Analyze requirements, identify files, and propose step-by-step approach.",
      "description": "Create implementation plan"
    },
    "team-spawn": {
      "template": "Spawn a new agent team using agent-teams plugin. Usage: team-spawn [preset|custom] [teamName]. Presets: review, debug, feature, fullstack, security, migration, research. Custom: comma-separated agent names.",
      "description": "Spawn multi-agent team"
    },
    "team-status": {
      "template": "Show status of active agent team using agent-teams plugin. Usage: team-status [teamName]. Shows agents, tasks, and progress.",
      "description": "Show team status"
    },
    "team-delegate": {
      "template": "Delegate task to specific agent in team using agent-teams plugin. Usage: team-delegate <teamName> <agentName> <subject> <description>.",
      "description": "Delegate to team agent"
    },
    "team-shutdown": {
      "template": "Shutdown agent team using agent-teams plugin. Usage: team-shutdown [teamName]. Shows summary of completed work.",
      "description": "Shutdown team"
    }
  },
  "agent": {
    "build": {
      "prompt_append": "## 완전한 팀 협업 시스템 (agent-teams + opencode-sessions)

OpenCode는 이제 Claude Code와 완벽히 동일한 팀 협업이 가능합니다:

### ✅ 주요 기능

| 기능 | 설명 |
|------|------|
| **실제 Agent 실행** | `session(agent, mode, task)` - 각 agent가 독립 session에서 작업 |
| **팀 리더** | team-workflow의 `team-spawn`으로 생성됨 |
| **작업 종속성** | blocks/blockedBy 필드로 작업 간 의존성 관리 |
| **에이전트 간 통신** | `team-send`, `team-broadcast` 명령으로 agent 간 메시지 전달 |
| **상태 확인** | `team-status`로 전체 팀 현황 파악 |
| **그레이스풀 종료** | `team-shutdown`, `team-approve-shutdown`으로 안전한 종료 |

### 📋 사용 명령

```bash
# 팀 생성 (모든 128 에이전트 또는 프리셋)
team-workflow spawn [preset|all] [teamName]

# 작업 추가 (종속성 추적 지원)
team-workflow add-task <team> "<subject>" "<description>" <assignee> <blocks> <blockedBy>

# 작업 완료
team-workflow complete <teamId> <taskId>

# 에이전트 통신
team-workflow send <team> <from> <to> "<message>"

# 상태 확인
team-workflow status [teamName]

# 방송
team-workflow broadcast <team> <from> "<message to all>"

# 종료
team-workflow shutdown <teamName>
team-workflow approve-shutdown <teamName>
```

### 📚 상세정보는 `~/.opencode/team-tasks/`에 저장됨

### 🔗 관련 문서
- 자세한 가이드: `AGENT-TEAMS.md`
- 워크플로우 문서: `TEAM-WORKFLOW.md`

---

### Claude Code vs OpenCode (완전한 팀 협업)

| 구분 | Claude Code | OpenCode |
|------|--------------|-----------|
| Agent 실행 방식 | Task.spawnteams() | session() 함수 |
| 팀 리더 | 전용 orchestrator | Build agent (조율 역할) |
| 작업 저장 | ~/.claude/teams/ | ~/.opencode/team-tasks/ |
| 에이전트 통신 | SendMessage (구조화됨) | team-send/broadcast |
| 종속성 추적 | ✅ blocks/blockedBy | ✅ blocks/blockedBy |

OpenCode에서 Claude Code 수준의 팀 협업을 경험할 수 있습니다!"
    }
  }
}
